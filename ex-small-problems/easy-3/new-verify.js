

console.log(Math.ceil(101 / 100)); // find century

/*
let century = Math.ceil(number/100);

let strCentury = String(century);
(prob same)
let strNumber = String(number);
let ordinal;
if (strNumber.length === 2 && strNumber[0] === 1) {
  ordinal = 'th';
} else if (strNumber[strNumber.length-1] === 2) {
  ordinal = 'nd';
} else if (strNumber[strNumber.length-1] === 3) {
  ordinal = 'rd';
} else {
  ordinal = 'th';
}


if (strCentury[strCentury.length-1] ===)
*/

console.log(typeof 'okay'.length);