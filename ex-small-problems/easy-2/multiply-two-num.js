/*
Create a function that takes two arguments, 
multiplies them together, and returns the result.
*/

function multiply(int1, int2) {
  return int1 * int2; 
}

console.log(multiply(2, 10)); 

console.log(multiply(5, 3) === 15); // logs true
/*
function are values;
they can be assigned, copied, or declared
and therefore can also be used to compare with other values
using comparison operators, like above:

[function return] === value; 
 */
